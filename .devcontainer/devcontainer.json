{
    "name": "Python 3",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "runArgs": [
        "-u",
        "vscode",
        "--env-file",
        "../.env"
    ],
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    }
}
